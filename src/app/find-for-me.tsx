import { ScreenStub } from '@/components/dev/ScreenStub';

export default function FindForMe() {
  return (
    <ScreenStub
      name="Find for Me"
      figmaNode="13:2611"
      note="Occasion / Mood / Budget chips → 'Show me ✦' → recommended restaurants."
      links={[{ href: '/restaurant/demo', label: 'Restaurant Detail (result card)' }]}
    />
  );
}
