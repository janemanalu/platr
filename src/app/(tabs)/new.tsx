// Placeholder route for the center tab slot. The tab bar button opens the Log a
// Visit modal instead of navigating here, so this is never actually shown.
export default function Never() {
  return null;
}
